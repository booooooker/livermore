# 聊天记录生成器（Spring Boot）

这是一个前后端不分离的 Spring Boot 工程，使用 Thymeleaf 渲染页面。

## 功能

- 在同一个页面输入对话文本
- 点击按钮后生成聊天记录
- 支持 `角色: 内容` 或 `角色：内容` 格式
- 自动附带生成时间

## 运行方式

```bash
mvn spring-boot:run
```

启动后访问：

- http://localhost:8080/

## 输入示例

```text
小明: 你好
小红: 你好呀
系统：欢迎使用聊天记录生成器
```
